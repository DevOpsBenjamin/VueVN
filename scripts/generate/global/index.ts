// Generated index for global location
import type { LocationData } from '@generate/types';
import { actionsList } from './actions';
import { eventsList } from './events';

const global: LocationData = {
  id: "global",
  actions: actionsList,
  events: eventsList,
  accessibles: {}
};

export default global;
