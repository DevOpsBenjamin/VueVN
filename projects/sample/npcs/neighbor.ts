import type { Neighbor_NPC } from "@/generate/types";

const neighbor: Neighbor_NPC = {
  name: "Sarah",
  flags: {},
  relation: 0,
  trust: 0,
  relationshipStatus: 'stranger',
  lastInteraction: undefined,
  favoriteTopics: ['books', 'coffee', 'gardening'],
};

export default neighbor;
