// Location configuration for city
import type { Location } from '@/generate/types';

// This will be populated by the existing location definitions
// The build system will merge this with the existing location data
export const locationConfig = {
  id: 'city',
  // Add any location-specific configuration here
};

export default locationConfig;
