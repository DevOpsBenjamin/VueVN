import type { Location } from '@generate/types';

const info: Location = {
  name: "Outside Home",
  baseBackground: "/outside/images/background/day.png", // Placeholder
  timeBackgrounds: [],
  unlocked: () => true,
  accessErrors: []
};

export default info;
